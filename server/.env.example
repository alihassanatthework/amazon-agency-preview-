# Express API
PORT=4000

# MongoDB connection string. The API starts without it, but submissions then
# return 503 rather than reporting a false success.
MONGO_URI=mongodb://127.0.0.1:27017/northbeam

# Restrict CORS in production to the deployed client origin.
CLIENT_ORIGIN=http://localhost:5173
